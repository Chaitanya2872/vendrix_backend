fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
celery==5.4.0
redis==5.2.1
boto3==1.35.90
minio==7.2.12
pymupdf==1.24.14
pillow==11.0.0
numpy==2.2.0
opencv-python-headless==4.10.0.84
# OCR runtime. `rapidocr` runs the PP-OCR detection and recognition networks
# under ONNX Runtime and is the default backend (OCR_BACKEND=onnx): same
# models as paddle, ~7x faster per page on CPU and ~20x cheaper to construct.
# See docs/ocr-performance.md for the measurements.
rapidocr==3.9.2
onnxruntime==1.29.0
# PaddleOCR is retained as the fallback backend (OCR_BACKEND=paddle) and is
# still what the ANPR path uses.
paddleocr==3.7.0
paddlepaddle==3.3.1
ultralytics==8.3.61
openpyxl==3.1.5
reportlab==4.2.5
python-docx==1.1.2
# Imported by app/modules/invoices/parsers (text_extraction, invoice_field_parser)
# but previously undeclared - the invoice pipeline fails at import without them.
pdfplumber==0.11.10
python-dateutil==2.9.0.post0
# OCR field-extraction model (app/ml/ocr): training and inference.
scikit-learn==1.9.0
joblib==1.5.3
# PostgreSQL driver. The compose deployment points DATABASE_URL at
# postgresql+psycopg2://, which fails at startup without this — and fails in
# a way that looks like a database outage rather than a missing package.
# SQLite deployments never import it.
psycopg2-binary==2.9.10
